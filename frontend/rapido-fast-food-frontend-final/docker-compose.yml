services:
  front-fastfood:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: frontfastfood
    ports:
      - '4200:80'
    restart: always
    networks:
      - fast-food-networks 

# --- IL MANQUAIT CETTE SECTION EN BAS ---
networks:
  fast-food-networks :
    external: true